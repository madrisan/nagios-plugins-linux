- General:
  * Add cpuinfo_get_frequency_estimate()?
  * Dust socket API to get number of pins and package type?

- X86 platforms:
  * Intel sockets characterisation
  * Virtualizers detection (cpuinfo_get_virtualizer() API?)

- IA-64 Platforms:
  * Hyper-threading characterisation (try hint@pause?)
  * Dual-core characterisation (cpuid[3].family >= 0x20 - Montecito)
  * FreeBSD: determine cache hierarchy, clock frequency

- PowerPC platforms:
  * Use 64bit, altivec from OF tree?
  * NetBSD: how to access the OF tree? PVR from user-space?

- MIPS platforms:
  * General: improve characterisation of caches hierarchy
  * General: detect 64bit, simd, fpu
  * Linux: how to get PRiD register from user-space?

- New ports:
  * ARM
  * SPARC

- Bindings (stable API required for DSO support):
  * python
